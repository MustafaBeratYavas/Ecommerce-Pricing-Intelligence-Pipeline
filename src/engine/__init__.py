"""Execution-layer components for browser lifecycle and queue processing.

The engine package owns long-running runtime control flow, while product
resolution, parsing, and persistence stay behind service boundaries.
"""
